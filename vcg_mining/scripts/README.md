# Stage Script Note

This directory exists to keep the requested `vcg_mining/scripts/` stage layout
visible in the repository.

The executable workflow scripts for this stage are saved in the project-level
`scripts/` directory to match the project rule that reusable scripts live
there:

- `scripts/vcg_mining_array.slurm`
- `scripts/summarize_vcg_hits.py`
